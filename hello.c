#include <stdio.h>

int main(void) {
    int x; // 첫번째 정수
    int y; // 두번째 정수
    int sum; // 두 정수의 합

    x = 100;
    y = 200;

    sum = x+y;

    printf("두 수의 합 = %d \n %f", sum,x);
    return 0;
}